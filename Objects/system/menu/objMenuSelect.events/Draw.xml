<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<event category="DRAW" id="0">
  <actions>
    <action id="603" library="1">
      <!--action name: Code-->
      <kind>CODE</kind>
      <allowRelative>false</allowRelative>
      <question>false</question>
      <canApplyTo>true</canApplyTo>
      <actionType>CODE</actionType>
      <functionName/>
      <relative>false</relative>
      <not>false</not>
      <appliesTo>.self</appliesTo>
      <arguments>
        <argument kind="STRING">var i;

for (i = 1; i &lt;= 3; i += 1) {
    draw_set_color(c_black)
    draw_set_halign(fa_left)
    draw_set_valign(fa_top)
    draw_set_font(font24)
    // draw "Data x"
    draw_text(x + 21 + (i - 1) * 239, y + 9, str[i])
    draw_set_font(font12)
    // draw death count
    draw_text(x + (i - 1) * 239 + 10, y + 70, 'death:' + string(menu_death[i]))
    // draw time
    time[i] = menu_time[i] div 100
    hour[i] = time[i] div 3600
    atime[i] = time[i] mod 3600
    muni[i] = atime[i] div 60
    sec[i] = atime[i] mod 60
    draw_text(
        x + (i - 1) * 239 + 10,
        y + 90,
        'time:' + string(hour[i]) + ':' + string(muni[i]) + ':' + string(sec[i])
    )
    // draw boss&amp;item information
    if (file_exists(getSaveFile(i))) {
        draw_set_halign(fa_center)
        draw_set_font(font24)
        //draw clear
        if ((menu_clear[i] = 1)) {
            draw_text(x + (i - 1) * 239 + 65, y + 215, 'Clear!!')
        }
        //draw icon
        for (j = 1; j &lt;= 8; j += 1) {
            if (menu_boss[i, j] &amp;&amp; sprite_exists(bossicon[j])) {
                draw_sprite(
                    bossicon[j],
                    -1,
                    x + (i - 1) * 239 + 32 * ((j - 1) mod 4),
                    y + 128 + 32 * ((j - 1) div 4)
                )
            }
        }
    }
    if (i == select) {
        draw_sprite(sprPlayerRunning, -1, x + (i - 1) * 239 + 60, y + 313)
    }
}

</argument>
      </arguments>
    </action>
  </actions>
</event>
